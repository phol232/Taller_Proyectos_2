NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_APP_NAME=Planner UC

# Netlify + Cloudflare Tunnel:
# NEXT_PUBLIC_API_URL=https://api.tu-dominio.com
#
# El JWT viaja en cookie httpOnly gestionada por el backend; nunca en variables de entorno del frontend.
